
type Person = struct {
  adr String
  name String
}

func main() {
    p = new(Person)

    v = "Hello"
    w = "World"
    v += w
    s =  "aaa"
    p.name = "Jan"
    p.name+=w
    assert(p.name=="JanWorld", "Expected p.name=JanWorld, got '%s'", p.name)

}
