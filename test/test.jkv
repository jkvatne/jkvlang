
type Person = struct {
  adr String
  name String
}

func main() {
    p = new(Person)
    p.name = "Ola Olsen"
    p.name += "xxx"
    v = "Hello"
    p.name = v
    assert(p.name=="Hello", "Expected p.name='Hello', got %s", p.name)

    p = new(Person)
    p.name = "Jan"

}