
type State = struct {
    text String
    p I32
}

func main() {
    s = new(State)
    s.p=2
    s.text = "ABCDEF"
    s.text[s.p] = 'x'
    assert(s.text=="ABxDEF", "Expected s.text='ABxDEF', got '%s'", s.text)
}
