
type Person = struct {
  adr String
  name String
}

func main() {
    p = new(Person)
    p.name = "Vatne"
    p.name+='X'
    assert(p.name=="VatneX", "Expected p.name=='VatneX', got '%s'", p.name)
}
