type State = struct {
    text String
}

type RangeIterator = struct {
    start I32
    end I32
}


func main() {
    s = new(State)
    s.text="Hello"
    s.text[1] = 'h'
    print("s.text='%s'", s.text)
    s = new(State)
    s.text = "Goodby"
    print("s.text='%s'", s.text)

}