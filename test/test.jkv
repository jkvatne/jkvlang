type State = struct {
    text String
}

type RangeIterator = struct {
    start I32
    end I32
}


func main() {
    s=new(State)
    s.text ="AB"
    ch = 'C'
    s.text+=ch
    print("s.text='%s'", s.text)
    s.text+=ch
    print("s.text='%s'", s.text)
}