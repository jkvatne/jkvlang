
type Person = struct {
  adr String
  name String
}

func main() {
    p = new(Person)
    p.name = "Ola Olsen"
    p.name += "xxx"   // Needed
    v = "Hello"
    p.name = v        // Needed. Leaks 40
}