
func f(s String) String {
    return "XX"
}

func main() {
    w = "XXX"
    // Test concat three strings and assign to local variable
    s = "Hello "
    t = "World "
    w = s+t
    w = f("F")
    assert(w=="XX", "Expected w='XX*', got '%s'")
}
