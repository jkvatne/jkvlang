
func out(s String) {
  print(s)
}

func f(a Bool, c I32) I32 {
    if a ? c+=2 : c+=1
    return c
}

func g(a Bool, c I32) I32 {
    if a ? c+=2 : c+=1
    return c
}

func main() {
    out("Testing out()\n")
    print("Welcome from jkv!\n")
    z = f(true, 5)
    assert(z==7, "Should not be printed, assert(z==%d)", z)
    print("z=%d;  Should be 7\n", z)
    x = 1
    assert(x==2, "Intentionally failed assert(x==2)")
    x = x + 2
    assert(x==3, "Should not be printed, assert(x==3)")
    assert(f(true, g(false,2))==111, "Intentionally fail assert(f(true,2)==111)")
    assert(f(true, g(false,2))==5, "Should not be printed assert(f(true,2)==3)")
    assert(x, "Should not be printed, assert(x+1)")
    assert(f(false, 2)==99, "Intentionally failed assert(f(false,2)==104)")
    assert(f(true, 2)==4, "Should not be printed assert(f(true,2)==4)")
    assert(z==990, "Intentionally failed assert, assert(z==990)")
    assert(false, "Intentionally failed assert, assert(false),  with parameter 1234: %d", 1234)
    print("Goodbye from jkv")
}
