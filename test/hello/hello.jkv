
func f(a Bool, c I32) I32 {
    if a ? c+=1 : c+=2
    return c
}

func main() {
    print("Hello from jkv again")
    // z = f(true, 5)
    //assert(f(false, 2)==3, "Testing f(false,2)==%d inside assert",3)
    assert(false, "Testing assert %d", 1234)
    x = 1
    x = x - 1
    // assert(x==3, "Assert that 1+2=3")
    // assert(z==2, "Testing f()")
    assert(x + 1, "Should not be printed")
}
