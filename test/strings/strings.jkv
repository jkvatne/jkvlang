/*
func f1(s String) String {
    t = "f1 "
    return t+s
}

func f2(s String) String {
    t = " f2"
    return s+t
}

func f3(s String, t String) String {
  return s+t
}

func f0(s String) String {
    return "f0 "+s
}

func g1(a String, b String, c String) String {
    return a+b+c
}

func g2(s String, t String) String {
    return s+t
}
*/

func main() {

    x = 2
    y = 3
    z = x*y
    printf("z=%d\n", z)

/*
    printf("%s\n", f1("F1")+f2("F2"))

    printf("Start tests\n")
    w = f1("F1")+f2("F2")
    printf("%s\n", w)

    printf("1. Test concat three strings and assign to local variable")
    s = "Hello "
    t = "World "
    u = "and goodbye!"
    w = s+t+u
    printf("s+t+u='%s'\n", w)

    printf("2. Test asserts that fail compare strings that are different size or different content\n")
    assert(f0("xyz")=="f0 xyz", "Should not fail")
    //assert(f0("axyz")=="f0 xyz", "Should fail")
    //assert(f0("ayz")=="f0 xyz", "Should fail")

    printf("3. Test nested functions")
    w = g1("abc", g2("def", "ghi"),"jkl")
    printf("w='%s'\n", w)
    w = g2("abc","def")
    printf("w='%s'\n", w)

    printf("4. Test concat two variables\n")
    s = "Hello "
    t = "World "
    u = s + t
    printf("u='%s'\n", u)
    assert(u=="Hello World ")

    printf("5. Test freeing of temporary object returned from f0\n")
    w = f0("xyz")
    printf("w='%s'\n", w)

    printf("6. Test calling function in actual parameter\n")
    assert(f0("xyz")=="f0 xyz")

    printf("7. Test concatenation of constant and variable\n")
    s = "xyz"
    v = "pre-" + s
    assert(v=="pre-xyz")

    printf("8. Test concatenation of variable and constant overwriting old local var\n")
    s = "xyz"
    v = s+"-post"
    assert(v=="xyz-post")
    printf("5. Test concatenation of constant and constant\n")
    w = "pre-"+"post"
    assert(w=="pre-post")

    printf("9. Test calling functions with string parameters\n")
    assert(f0("xyz")=="f0 xyz")
    assert(f1("xyz")=="f1 xyz")
    assert(f2("xyz")=="xyz f2")

    printf("10. Test string compare equal\n")
    a = "abc"
    b = "abc"
    assert(a=="abc", "Failed a==abc")
    assert(a==b, "a==b failed")
    assert(b==a, "b==a failed")

    printf("11. Test string compare equal and not equal\n")
    a = "abc"
    c = "abcs"
    assert(a!=c, "a!=c failed")
    assert(c!=a, "c!=a failed")
    c = "abx"
    assert(a!=c, "a!=c failed")
    c = "ab"
    assert(a!=c, "a!=c failed")

    a = "abc"
    b = "abx"
    if a==b {
        assert(false,"Failed compare strings")
    }

    printf("12. Test tripple concat in assert")
    s = "Hello "
    t = "World "
    u = "and goodbye!"
    assert(s+t+u=="Hello World and goodbye!", "9. Failed: s+t+u")

    printf("13. Test concat three strings\n")
    w = s + t + u
    printf("w=%s\n", w)
    assert(w=="Hello World and goodbye!", "s+t+u failed")

    printf("14. Test string compare with literal\n")
    a = "abc"
    assert(a!="abb", "a!='abb' failed")
    assert("abca"!=a, "'cxba'!=a failed")

    printf("15. Test more concat\n")
    assert(s+"--"=="Hello --", "Failed s+'--'")
    assert(s+"--"+t+u=="Hello --World and goodbye!", "Failed 11")

    printf("16. Test concat function results\n")
    w = f1("F1")+f2("F2")
    printf("%s\n", f1("F1")+f2("F2"))

    assert(f1("xyz")=="f1 xyz")

    printf("End of tests\n")
    */
}
