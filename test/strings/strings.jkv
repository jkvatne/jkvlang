
func main() {
    printf("Starting string tests\n")
    a = "abc"
    b = "abc"
    printf("Has allocated a and b\n")
    /*
    assert(b==a, "b==a failed")
    assert(a==b, "a==b failed")
    c = "abcd"
    assert(a!=c, "a!=c failed")
    assert(c!=a, "c!=a failed")
    if a==c {
        assert(false,"Failed compare strings")
    }

    // Test concat
    s = "Hello "
    t = "World "
    v = s + t
    println(v)
    u = "and goodbye!"
    w = s + t + u
    println(w)
    println("Ending string tests")
    */
}
