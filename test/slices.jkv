
type Age = U8

 type Person = struct {
   name String
   age Age
 }

func main() {
/*
    p = new(Person)
    p.name = "Olsen"
    p.name[3]=120 //'x'
    p.age = 70
    print("%s %d", p.name, p.age)
    flush()
*/
    u = "abc"
    u[1]=120 // This should fail because u is a string literal (read only).

}