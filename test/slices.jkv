
type Age = U8

 type Person = struct {
   name String
   age Age
 }

func main() {
  /*
    u = "abc"
    u[1]=120 // 'x'
    assert(u=="axc")
    print("u=%s", u)
*/
    p = new(Person)
    p.name = "Olsen"
    p.name[1]=120
    p.age = 70
    print("%s %d", p.name, p.age)
}